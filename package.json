{
  "name": "macrospend-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "MacroSpend desktop tracker — native tray agent for macOS and Windows",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@tauri-apps/plugin-deep-link": "^2.0.0",
    "@tauri-apps/plugin-updater": "^2.0.0",
    "@tauri-apps/plugin-autostart": "^2.0.0"
  }
}
